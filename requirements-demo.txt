# VetQwen demo-only dependencies for local Mac usage
# Use this when you only need the Gradio demo and pulled adapter artifacts.
transformers==4.44.2
peft==0.12.0
accelerate==0.33.0
gradio>=4.40.0
requests>=2.32.0
numpy>=1.26.0
